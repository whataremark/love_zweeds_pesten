--- Basisconfiguratie voor kaarten en spelopzet ----------------------
return {
    cardBaseW   = 140,   -- design width (px)
    cardBaseH   = 200,   -- design height (px)
    cardPad     = 16,    -- spacing inside containers (px)
    safePad     = 16,    -- viewport safe-area margin (px)
    minScale    = 0.60,  -- minimum overall UI scale
    maxScale    = 1.25,  -- maximum overall UI scale
    phoneBP     = 800,   -- breakpoint width for compact layout (px)
    minTap      = 40,    -- minimum tappable target size at scale=1 (px)

    HAND_SIZE   = 6,
    CARDS_INHAND = 3,
    SETUP_OPEN  = 3,
    BLIND_SIZE  = 3,
}
